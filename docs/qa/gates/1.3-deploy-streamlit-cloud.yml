storyId: "1.3"
storyTitle: "Configurar e validar deploy no Streamlit Cloud"
verdict: PASS
gateDate: "2026-05-22"
agent: "@qa (Quinn)"

summary: >
  Story documental + configuração de deploy. Todos os 5 ACs verificados,
  DoD completo, sem código de produção de risco. Startup check implementado
  antes do auth guard, conforme design da story. Nenhuma issue crítica ou
  alta detectada.

checks:
  code_review:
    result: PASS
    notes: >
      app.py: startup check usa st.secrets.get() (não lança KeyError),
      variável sentinela _anthropic_api_key nunca é logada nem exibida
      (ADR-004 cumprido). st.stop() após st.error() previne renderização
      parcial. Import de audit_log correto (absoluto: from src.utils import
      audit_log). Sem imports relativos.

  unit_tests:
    result: PASS
    notes: >
      Story documental — escopo exclui testes automatizados de deploy.
      A Notas Técnicas documenta que a validação do startup check é feita
      via procedimento manual (Passo 5 do deploy.md). Comportamento correto
      para stories de infraestrutura/documentação.

  acceptance_criteria:
    result: PASS
    notes: |
      AC #1 — docs/guides/deploy.md criado: VERIFICADO (270 linhas,
        pré-requisitos, 5 passos, validação, troubleshooting, referências ADR).
      AC #2 — .streamlit/secrets.toml.example documenta todas as variáveis:
        VERIFICADO (ANTHROPIC_API_KEY, AUTH_COOKIE_KEY, MAX_COST_BRL com
        comentários por chave).
      AC #3 — App inicializa corretamente com ANTHROPIC_API_KEY presente:
        VERIFICADO por inspeção de código (st.secrets.get retorna valor,
        bloco if not é falso, execução continua).
      AC #4 — App exibe mensagem útil sem crashar quando chave ausente:
        VERIFICADO (st.error com orientação + st.stop(), nunca st.exception).
      AC #5 — README.md atualizado com link para deploy.md: VERIFICADO.

  no_regressions:
    result: PASS
    notes: >
      Startup check posicionado ANTES do require_authentication() — ordem
      lógica correta e não-conflitante com as mudanças da Story 1.12
      (sidebar download button vem depois do auth guard). 260 testes
      passando conforme Notas de Implementação.

  performance:
    result: PASS
    notes: >
      Story documental. Startup check usa st.secrets.get() — O(1), sem
      I/O extra no critical path.

  security:
    result: PASS
    notes: >
      ANTHROPIC_API_KEY nunca logada, nunca exibida em st.error nem em
      mensagem de ajuda (valor mascarado). Orientação de "não commitar
      secrets.toml" presente em deploy.md e no próprio arquivo de exemplo.
      Estratégia A de config.yaml (recomendada) evita versionar credenciais
      do auth.

  documentation:
    result: PASS
    notes: >
      docs/guides/deploy.md completo e bem estruturado: pré-requisitos,
      passo a passo, troubleshooting, próximos passos, referências ADR.
      secrets.toml.example com comentários inline por chave (obrigatório/
      opcional). README com seção "Deploy" e link para o guia.

issues: []

adrs_verified:
  - id: ADR-003
    status: compliant
    note: "Referenciado em deploy.md — política de Temperature"
  - id: ADR-004
    status: compliant
    note: "API key nunca logada. Retenção zero documentada em deploy.md"
  - id: ADR-005
    status: compliant
    note: "MAX_COST_BRL presente em secrets.toml.example com comentário"
