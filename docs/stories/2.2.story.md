# Story 2.2 — Implementar `batch_processor.py` — Orquestrador de lotes

**Status:** Done
**Sprint:** 2 — Pipeline de batch de correção
**Complexidade:** M
**Prioridade:** Alta
**Dependências:** Story 2.1 (clone_client.py implementado)

---

## Contexto

O ADR-002 define o padrão Wrapper Python como única forma de integração com o squad de correção. A Story 2.1 implementa `clone_client.py`, responsável por uma única chamada à API Anthropic para um lote de alunos. Esta story implementa a camada acima: o orquestrador `batch_processor.py`, que recebe a lista completa de alunos vinda da Aba 1 da UI, particiona em lotes de `BATCH_SIZE=10` (ADR-002, veto V8), distribui o trabalho via `ThreadPoolExecutor`, persiste estado intermediário em sqlite3 efêmero e retorna os resultados consolidados para a UI.

O ADR-005 define a política de orçamento: estimativa calculada **antes** de qualquer chamada à API, com bloqueio automático (`OrcamentoExcedidoError`) se o custo estimado ultrapassar `MAX_COST_BRL`. Isso garante que nenhuma chamada de produção ocorra sem aprovação prévia do PA.

O Sprint 1 registrou o item de débito técnico MNT-003: strings livres sendo passadas como `acao` para `log_action()`. Esta story resolve esse débito introduzindo o enum `AcaoBatch` como contrato formal para todos os payloads de log do batch.

---

## Objetivo

Implementar `src/batch/batch_processor.py` — orquestrador que recebe a lista de alunos (da Aba 1), particiona em lotes de `BATCH_SIZE=10`, chama `clone_client.corrigir_aluno()` via `ThreadPoolExecutor`, persiste estado intermediário em sqlite3 efêmero e retorna resultados para a UI. A estimativa de custo é calculada previamente (ADR-005) e o processamento é bloqueado se o custo estimado exceder `MAX_COST_BRL`.

---

## Acceptance Criteria

- [x] **AC-01:** `processar_batch(alunos, enunciado, metadata)` particiona a lista em lotes de `BATCH_SIZE=10` (do ADR-002) — sem hard-code do valor 10 diretamente no código; usa a constante `BATCH_SIZE` importada ou definida no módulo
- [x] **AC-02:** Processamento de lotes usa `ThreadPoolExecutor` com `max_workers` configurável (default 3)
- [x] **AC-03:** Estado intermediário de cada lote é persistido via funções públicas de `batch_state` (`criar_batch`, `salvar_lote`, `marcar_lote_falha`). `batch_processor.py` NUNCA acessa sqlite3 diretamente — toda persistência é delegada ao módulo `src/batch/batch_state.py` (Story 2.6). O comentário EFÊMERO e o `DB_PATH=/tmp/` são responsabilidade de `batch_state.py`, já implementado.
- [x] **AC-04:** Falha de um lote (`ClonValidationError` ou `ClonTruncatedResponseError`) NÃO aborta os demais lotes — a exceção é capturada, registrada no banco e o processamento continua nos lotes restantes
- [x] **AC-05:** `ClonTruncatedResponseError` marca os alunos afetados como `"status": "REVISÃO MANUAL — TRUNCADA"` no banco sqlite3
- [x] **AC-06:** `ClonValidationError` marca os alunos afetados como `"status": "REVISÃO MANUAL — VALIDAÇÃO"` no banco sqlite3
- [x] **AC-07:** Estimativa de custo é calculada ANTES de qualquer chamada à API, usando a fórmula do ADR-005 (`N_lotes × custo_haiku + 1 × custo_sonnet + N_regeneracoes × custo_regen`), e retornada junto com os resultados finais
- [x] **AC-08:** Se `custo_estimado_brl > MAX_COST_BRL`, o processamento é BLOQUEADO antes de qualquer chamada à API — levanta `OrcamentoExcedidoError` com mensagem clara conforme ADR-005 (inclui: estimativa calculada, hard limit, excesso, instruções para ajuste)
- [x] **AC-09:** `MAX_COST_BRL` lido de `st.secrets.get("MAX_COST_BRL", "15.0")` com fallback para `15.0` (padrão ADR-005; compatível com Story 1.3 — leitura de secrets via Streamlit)
- [x] **AC-10:** MNT-003 do Sprint 1 resolvido: `acao` passada ao `log_action()` usa constantes do enum `AcaoBatch` (não strings livres) — implementar `AcaoBatch(str, Enum)` em `src/batch/batch_processor.py`
- [x] **AC-11:** `log_action()` chamado em: início do batch, por lote processado (com numero do lote e total), e ao final do batch — payloads sem PII (ex: `"lote 3/12, 10 alunos"`, não nomes nem RAs)
- [x] **AC-12:** `pytest tests/unit/test_batch_processor.py` passa com mocks de `clone_client.corrigir_aluno`
- [x] **AC-13:** `processar_batch()` usa `threading.Lock` de módulo (`_db_write_lock`) para serializar todas as chamadas de escrita a `batch_state` (`salvar_lote`, `marcar_lote_falha`). `batch_state.inicializar_banco()` ativa `PRAGMA journal_mode=WAL` — patch aplicado em `batch_state.py` como parte desta story (ver AC-14).
- [x] **AC-14:** `batch_state.py` (Story 2.6) recebe dois patches autorizados pelo @architect como parte desta story: (1) `inicializar_banco()` adiciona `conn.execute("PRAGMA journal_mode=WAL")` antes dos DDLs; (2) nova função `criar_batch(sessao_id: str, total_alunos: int) -> int` — insere na tabela `batches` e retorna `lastrowid`. Estes patches não alteram nenhum AC da Story 2.6.
- [x] **AC-15:** Erros de rate limiting HTTP 429 em `corrigir_aluno()` são tratados com backoff exponencial simples: até 2 retries com `time.sleep(10 * 2 ** tentativa)` segundos (10s, 20s). Após 2 retries sem sucesso, o lote é marcado via `marcar_lote_falha()` com motivo `"rate_limit_esgotado"`. Nenhuma nova dependência (tenacity ou similar) é adicionada.
- [x] **AC-16:** O `batch_processor.py` converte o retorno de `corrigir_aluno()` (lista de dicts) em objetos com atributo `.ra` usando `types.SimpleNamespace` antes de passar para `salvar_lote()`. Não importa `FichaCorrecao` diretamente para evitar dependência circular entre camadas.

---

## Escopo

**Dentro do escopo:**
- `src/batch/batch_processor.py` — implementação completa do orquestrador
- `src/batch/__init__.py` — exposição dos símbolos públicos do módulo
- `src/batch/exceptions.py` — definição de `OrcamentoExcedidoError`
- `tests/unit/test_batch_processor.py` — mínimo 8 testes cobrindo: partição de lotes, falha isolada por lote, bloqueio por orçamento, persistência sqlite3, log_action com enum `AcaoBatch`

**Fora do escopo:**
- UI/Streamlit do batch — story separada (Sprint 2)
- `calibrator.py` — story separada (Sprint 2)
- Integração real com API Anthropic (todos os testes usam mocks)
- Lógica de exibição da estimativa na UI (responsabilidade da story de UI)

---

## Padrão sqlite3 OBRIGATÓRIO

O seguinte trecho DEVE aparecer no código exatamente com este comentário e este caminho:

```python
# EFÊMERO: dados perdidos ao reiniciar sessão — comportamento esperado (ADR-004)
DB_PATH = Path("/tmp/batch_state.db")  # NUNCA usar caminho relativo ao projeto
```

**Motivação:** o Streamlit Cloud free tier usa filesystem efêmero destruído a cada redeploy. sqlite3 aqui serve exclusivamente como estado intermediário de sessão de batch — não como persistência cross-session. Uso de `/tmp/` é intencional e documentado.

**Anti-padrão proibido:**
```python
# ERRADO — sobrevive ao redeploy apenas localmente; perde dados em produção silenciosamente
DB_PATH = Path("batch_state.db")
```

---

## Notas de Implementação

### Estrutura da função principal

```python
def processar_batch(
    alunos: list[dict],
    enunciado: str,
    metadata: dict,
    max_workers: int = 3,
    n_regeneracoes: int = 3,
) -> dict:
    """
    Orquestra a correção em lotes.

    Returns:
        {
            "resultados": [...],      # lista de resultados por aluno
            "custo_estimado_brl": float,
            "lotes_processados": int,
            "lotes_com_erro": int,
        }

    Raises:
        OrcamentoExcedidoError: se custo_estimado_brl > MAX_COST_BRL
    """
```

### Enum AcaoBatch (resolve MNT-003)

```python
from enum import Enum

class AcaoBatch(str, Enum):
    BATCH_INICIADO = "batch_iniciado"
    LOTE_PROCESSADO = "lote_processado"
    LOTE_ERRO = "lote_erro"
    BATCH_CONCLUIDO = "batch_concluido"
```

**Uso correto (MNT-003 resolvido):**
```python
log_action(acao=AcaoBatch.BATCH_INICIADO, payload={"total_alunos": len(alunos), "n_lotes": n_lotes})
```

**Anti-padrão proibido (era MNT-003):**
```python
log_action(acao="batch_iniciado", ...)  # ERRADO — string livre
```

### Fórmula de estimativa (ADR-005)

```python
PRECO_HAIKU_IN  = 0.80   # USD por 1M tokens de entrada
PRECO_HAIKU_OUT = 4.00   # USD por 1M tokens de saída
PRECO_SONNET_IN = 3.00   # USD por 1M tokens de entrada
PRECO_SONNET_OUT = 15.00 # USD por 1M tokens de saída
TAXA_CAMBIO     = 5.50   # USD → BRL (referência maio/2026 — ADR-005)
TOKENS_IN_HAIKU = 4_000
TOKENS_OUT_HAIKU = 4_096
TOKENS_IN_SONNET = 3_000
TOKENS_OUT_SONNET = 4_096

def estimar_custo_brl(n_lotes: int, n_regeneracoes: int = 3) -> float:
    custo_haiku  = n_lotes        * (TOKENS_IN_HAIKU  * PRECO_HAIKU_IN  + TOKENS_OUT_HAIKU  * PRECO_HAIKU_OUT)  / 1_000_000
    custo_sonnet = 1              * (TOKENS_IN_SONNET * PRECO_SONNET_IN + TOKENS_OUT_SONNET * PRECO_SONNET_OUT) / 1_000_000
    custo_regen  = n_regeneracoes * (TOKENS_IN_HAIKU  * PRECO_HAIKU_IN  + TOKENS_OUT_HAIKU  * PRECO_HAIKU_OUT)  / 1_000_000
    total_usd = custo_haiku + custo_sonnet + custo_regen
    return total_usd * TAXA_CAMBIO
```

### Leitura de MAX_COST_BRL

```python
import streamlit as st

def _get_max_cost_brl() -> float:
    try:
        return float(st.secrets.get("MAX_COST_BRL", "15.0"))
    except (ValueError, TypeError):
        # valor inválido → usa padrão e loga aviso (ADR-005)
        return 15.0
```

### Schema sqlite3 sugerido

```sql
CREATE TABLE IF NOT EXISTS batch_lotes (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    lote_num    INTEGER NOT NULL,
    total_lotes INTEGER NOT NULL,
    status      TEXT    NOT NULL,  -- 'OK', 'REVISÃO MANUAL — TRUNCADA', 'REVISÃO MANUAL — VALIDAÇÃO'
    alunos_json TEXT,              -- JSON dos resultados do lote (sem PII desnecessária)
    criado_em   TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Isolamento de falhas por lote (AC-04)

Cada future do `ThreadPoolExecutor` deve ser envolvido em try/except dentro do loop de coleta de resultados. `ClonValidationError` e `ClonTruncatedResponseError` devem ser capturados individualmente — outros erros inesperados podem ser re-levantados ou registrados dependendo da política de logging.

### Referências ao código existente

- `clone_client.corrigir_aluno(alunos_lote, enunciado, metadata)` — interface definida na Story 2.1
- `log_action()` — importado de `src/utils/logger.py` (Story 1.6)
- `ClonValidationError`, `ClonTruncatedResponseError` — definidos em `packages/wrapper/exceptions.py` (Stories 2.1 / 1.x)

---

## Casos de Teste Obrigatórios (mínimo 8)

### TC-01 — Partição correta em lotes de BATCH_SIZE

```
Arrange: lista de 25 alunos mock
Act: processar_batch(alunos, enunciado, metadata) com mock de corrigir_aluno
Assert: corrigir_aluno chamado 3 vezes (lotes de 10, 10, 5)
```

### TC-02 — Falha em lote não aborta os demais

```
Arrange: lista de 20 alunos; corrigir_aluno levanta ClonValidationError no lote 1
Act: processar_batch(alunos, enunciado, metadata)
Assert: não levanta exceção; resultado contém 10 alunos com status "REVISÃO MANUAL — VALIDAÇÃO" e 10 com status "OK"
```

### TC-03 — ClonTruncatedResponseError marca status correto

```
Arrange: lista de 10 alunos; corrigir_aluno levanta ClonTruncatedResponseError
Act: processar_batch(alunos, enunciado, metadata)
Assert: todos os 10 alunos têm status "REVISÃO MANUAL — TRUNCADA" nos resultados
```

### TC-04 — Bloqueio por orçamento (AC-08)

```
Arrange: lista de 1000 alunos (custo estimado alto); MAX_COST_BRL = 1.00
Act + Assert:
  with pytest.raises(OrcamentoExcedidoError) as exc_info:
      processar_batch(alunos, enunciado, metadata)
  assert "ORÇAMENTO EXCEDIDO" in str(exc_info.value)
  assert corrigir_aluno nunca foi chamado (mock.call_count == 0)
```

### TC-05 — Estimativa dentro do limite não bloqueia

```
Arrange: lista de 10 alunos; MAX_COST_BRL = 50.00
Act: processar_batch(alunos, enunciado, metadata) com mock de corrigir_aluno
Assert: não levanta OrcamentoExcedidoError; resultado contém chave "custo_estimado_brl"
```

### TC-06 — Estado persistido em sqlite3

```
Arrange: lista de 10 alunos; mock de corrigir_aluno retorna resultado válido
Act: processar_batch(alunos, enunciado, metadata)
Assert: arquivo /tmp/batch_state.db existe após execução; contém pelo menos 1 linha na tabela batch_lotes
```

### TC-07 — log_action chamado com enum AcaoBatch (resolve MNT-003)

```
Arrange: mock de log_action e de corrigir_aluno; lista de 10 alunos
Act: processar_batch(alunos, enunciado, metadata)
Assert:
  - log_action chamado ao menos 3 vezes (início, lote, fim)
  - todos os calls usam instâncias de AcaoBatch, não strings livres
  - nenhum payload contém RA, nome ou nota de aluno
```

### TC-08 — max_workers configurável (AC-02)

```
Arrange: mock de corrigir_aluno; lista de 30 alunos; max_workers=2
Act: processar_batch(alunos, enunciado, metadata, max_workers=2)
Assert: processamento completa sem erro; ThreadPoolExecutor foi criado com max_workers=2
  (verificável via mock de concurrent.futures.ThreadPoolExecutor)
```

---

## Riscos

- **Risco 1:** Story 2.1 (`clone_client.py`) pode não estar Done quando esta story for iniciada. Mitigação: esta story depende explicitamente de 2.1 — o `@dev` deve verificar a existência e interface de `clone_client.corrigir_aluno()` antes de implementar. Se 2.1 estiver em InProgress, pode-se criar um stub mínimo e prosseguir com mocks nos testes.
- **Risco 2:** `st.secrets` não disponível fora do contexto Streamlit (ex: ao rodar testes unitários). Mitigação: `_get_max_cost_brl()` usa try/except e os testes devem monkeypatchar essa função para injetar valores de teste diretamente.
- **Risco 3:** Condições de corrida no sqlite3 com `ThreadPoolExecutor`. Mitigação: usar `threading.Lock` para serializar escritas no banco ou usar WAL mode (`PRAGMA journal_mode=WAL`).
- **Risco 4:** Preços Anthropic podem mudar — constantes de estimativa ficarão desatualizadas. Mitigação: documentadas com data de referência (ADR-005); ADR deve ser revisitado a cada 6 meses.

---

## Definição de Pronto (DoD)

- [x] AC-01 a AC-12 verificados
- [x] `pytest tests/unit/test_batch_processor.py` passa com ≥ 8 testes, 0 FAILED (10/10 PASSED)
- [x] `ruff check src/batch/` clean (sem warnings) — All checks passed
- [x] sqlite3 path usa `/tmp/` com comentário EFÊMERO obrigatório exatamente conforme SPRINT-2-INDEX.md (responsabilidade de `batch_state.py` — já cumprido pela Story 2.6)
- [x] Nenhum RA, nome ou nota em payloads de `log_action()` — verificável nos asserts do TC-07
- [x] `AcaoBatch(str, Enum)` implementado em `src/batch/batch_processor.py` — MNT-003 resolvido
- [x] `OrcamentoExcedidoError` definido em `src/batch/exceptions.py`
- [x] Status atualizado para InReview ao concluir
- [x] File List e Change Log atualizados

---

## Referências ADR

| ADR | Seção relevante | Impacto nesta story |
|-----|----------------|---------------------|
| [ADR-002](../decisions/ADR-002-clone-contract.md) | Veto V8 — `BATCH_SIZE=10` | Constante `BATCH_SIZE` — proibido hard-code do valor 10 |
| [ADR-003](../decisions/ADR-003-temperature-policy.md) | `max_tokens=4096`, `temperature=0`, modelo Haiku | Constantes da fórmula de estimativa |
| [ADR-004](../decisions/ADR-004-lgpd-retention.md) | Retenção efêmera / LGPD | Comentário EFÊMERO obrigatório no DB_PATH |
| [ADR-005](../decisions/ADR-005-budget-policy.md) | Fórmula de estimativa, `MAX_COST_BRL`, mensagem de bloqueio | AC-07, AC-08, AC-09 |

**Débito técnico resolvido:**
- MNT-003 (Sprint 1): `acao` passada ao `log_action()` com strings livres → resolvido com `AcaoBatch(str, Enum)` (AC-10)

---

## File List

**A criar:**
- `src/batch/__init__.py`
- `src/batch/batch_processor.py`
- `src/batch/exceptions.py`
- `tests/unit/test_batch_processor.py`

**Criados nesta story:**
- `src/batch/__init__.py` — exporta `AcaoBatch`, `OrcamentoExcedidoError`, `estimar_custo_brl`, `processar_batch`
- `src/batch/batch_processor.py` — orquestrador completo (`processar_batch`, `estimar_custo_brl`, `_get_max_cost_brl`, `AcaoBatch`, `_db_write_lock` de módulo, retry 429 com backoff, conversão `SimpleNamespace`)
- `src/batch/exceptions.py` — `OrcamentoExcedidoError` (mensagem ADR-005 com estimativa/limite/excesso/instruções)
- `tests/unit/test_batch_processor.py` — 10 testes (TC-01 a TC-10) cobrindo todos os ACs

**Referenciados (não modificados):**
- `packages/wrapper/clone_client.py` — interface `corrigir_aluno()` consumida por este módulo (Story 2.1)
- `packages/wrapper/exceptions.py` — `ClonValidationError`, `ClonTruncatedResponseError`
- `src/utils/logger.py` — `log_action()` (Story 1.6)
- `docs/decisions/ADR-002-clone-contract.md` — `BATCH_SIZE`, veto V8
- `docs/decisions/ADR-003-temperature-policy.md` — `max_tokens`, modelo Haiku
- `docs/decisions/ADR-004-lgpd-retention.md` — política de retenção efêmera
- `docs/decisions/ADR-005-budget-policy.md` — fórmula de estimativa, `MAX_COST_BRL`

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-05-22 | @sm (River) | Story criada — orquestrador de lotes com ThreadPoolExecutor, sqlite3 efêmero e guardrails ADR-005 |
| 2026-05-22 | @po (Pax) | Validação *validate-story-draft — GO (score 97/100). Status: Draft → Ready |
| 2026-05-22 | @architect (Aria) + @sm (River) | Revisão pré-dev: 4 decisões arquiteturais — AC-03 substituído, AC-13 a AC-16 adicionados. Patches cirúrgicos aplicados em batch_state.py (WAL mode + criar_batch()). |
| 2026-05-22 | @dev (Dex) | Status: Ready → InProgress. Início da implementação em modo YOLO. |
| 2026-05-22 | @dev (Dex) | Implementação concluída: `src/batch/batch_processor.py`, `src/batch/exceptions.py` (`OrcamentoExcedidoError`), `src/batch/__init__.py` (exports), `tests/unit/test_batch_processor.py` (10 testes — TC-01 a TC-10, todos PASSED). Todos os 16 ACs verificados; `ruff check src/batch/` clean; full test suite 283 passed / 2 skipped (sem regressões). AC-14: patches em `batch_state.py` (WAL mode em `inicializar_banco()` + função `criar_batch()`) foram aplicados pela Story 2.6 previamente — confirmado e reutilizado, sem reaplicar. Status: InProgress → InReview. |
| 2026-05-22 | @qa (Quinn) | QA Gate PASS — 16/16 ACs verificados contra código real; 10/10 testes PASSED; ruff clean; full suite 283 passed / 2 skipped (sem regressões). MNT-003 resolvido (AcaoBatch(str, Enum)); patches AC-14 em batch_state.py confirmados; backoff 10s/20s (AC-15) e conversão SimpleNamespace (AC-16) implementados corretamente. 3 observações low-severity registradas no gate (MNT-004, ARCH-001, REL-001) — não bloqueantes. Status: InReview → Done. |

---

## QA Results

### Review Date: 2026-05-22

### Reviewed By: Quinn (Test Architect & Quality Advisor)

### Veredicto: **PASS**

### Resumo Executivo

Story 2.2 implementa de forma completa e correta o orquestrador `batch_processor.py`. Os 16 ACs foram verificados contra o código real (não apenas contra documentação), 10/10 testes unitários passam, `ruff` está clean, a suíte completa (283 passed / 2 skipped) não apresenta regressões, e o débito técnico MNT-003 do Sprint 1 foi efetivamente resolvido. Implementação alinhada aos ADRs 002, 003, 004 e 005.

### 7 Quality Checks

| Check | Status | Notas |
|-------|--------|-------|
| Code Review | PASS | Imports limpos; separação clara de responsabilidades; AcaoBatch resolve MNT-003; conversão SimpleNamespace bem isolada; retry com escalada determinística. |
| Unit Tests | PASS | 10/10 PASSED (TC-01 a TC-10). Cobertura ampla incluindo guardrail de orçamento, isolamento de falhas, persistência, log sem PII e rate-limit retry. |
| Acceptance Criteria | PASS | AC-01 a AC-16 verificados contra código real. Todos atendidos. |
| No Regressions | PASS | Suíte completa: 283 passed / 2 skipped. |
| Performance | PASS | ThreadPoolExecutor com max_workers configurável; estimativa O(1); lock apenas em escritas. |
| Security | PASS | Sem PII (RA/nome/nota) em payloads de log (TC-07 verifica via regex); DB_PATH efêmero documentado (ADR-004); OrcamentoExcedidoError previne abuso financeiro. |
| Documentation | PASS | Docstrings completas; comentários referenciam ADRs; File List e Change Log atualizados. |

### Verificação AC-a-AC

| AC | Status | Evidência |
|----|--------|-----------|
| AC-01 | PASS | `_particionar(alunos, BATCH_SIZE)` (linha 284); `BATCH_SIZE` importado de `clone_client` (linha 31) — sem hard-code de 10 |
| AC-02 | PASS | `ThreadPoolExecutor(max_workers=max_workers)` (linha 313), default 3 (linha 261); TC-08 verifica |
| AC-03 | PASS | Toda persistência via `batch_state.criar_batch/salvar_lote/marcar_lote_falha`. Nenhum `import sqlite3` em `batch_processor.py` |
| AC-04 | PASS | `ClonValidationError` e `ClonTruncatedResponseError` capturados individualmente no loop `as_completed` (linhas 333, 348) |
| AC-05 | PASS | `STATUS_TRUNCADA = "REVISÃO MANUAL — TRUNCADA"` aplicado no branch de truncamento |
| AC-06 | PASS | `STATUS_VALIDACAO = "REVISÃO MANUAL — VALIDAÇÃO"` aplicado no branch de validação |
| AC-07 | PASS | `estimar_custo_brl()` invocada ANTES de qualquer chamada API (linha 288); valor retornado em `result["custo_estimado_brl"]` |
| AC-08 | PASS | `if custo_estimado > limite: raise OrcamentoExcedidoError(...)` (linha 290-291); mensagem ADR-005 completa (estimativa/limite/excesso/instruções) |
| AC-09 | PASS | `float(st.secrets.get("MAX_COST_BRL", "15.0"))` com fallback 15.0 (linha 127) |
| AC-10 | PASS | `class AcaoBatch(str, Enum)` (linha 53) — 4 membros conforme spec; MNT-003 resolvido |
| AC-11 | PASS | `log_action()` invocado em início/por-lote/fim; payloads sem PII (TC-07 verifica via regex) |
| AC-12 | PASS | `pytest tests/unit/test_batch_processor.py` → 10/10 PASSED |
| AC-13 | PASS | `_db_write_lock = threading.Lock()` (linha 45); `with _db_write_lock:` envolve TODAS as escritas em batch_state |
| AC-14 | PASS | `batch_state.py`: `PRAGMA journal_mode=WAL` (linha 65); `criar_batch(sessao_id, total_alunos)` (linha 72) retorna `lastrowid` |
| AC-15 | PASS | `_processar_um_lote` (linhas 230-249): 3 tentativas; `time.sleep(10 * 2**tentativa)` = 10s, 20s; falha → motivo `rate_limit_esgotado`; nenhuma nova dependência; TC-10 valida |
| AC-16 | PASS | `_to_simple_ns()` (linha 153) usa `types.SimpleNamespace`; `FichaCorrecao` NÃO importado em `batch_processor.py` |

### Issues Identificadas (não bloqueantes)

1. **MNT-004 (low)** — `_get_max_cost_brl()` linha 128 captura `(ValueError, TypeError, AttributeError, FileNotFoundError, Exception)`. Como `Exception` já cobre os tipos anteriores, é redundante (mas já com `# noqa: BLE001`). **Sugestão:** simplificar para `except Exception:` ou usar `contextlib.suppress` em iteração futura.

2. **ARCH-001 (low)** — Em `processar_batch()` linhas 298-300, a sequência `inicializar_banco() → limpar_banco() → inicializar_banco()` contém uma chamada inicial possivelmente redundante (idempotência). **Sugestão:** avaliar remoção da primeira chamada em refator futuro.

3. **REL-001 (low)** — `_db_write_lock` é módulo-scoped, serializando escritas globalmente entre invocações concorrentes de `processar_batch()`. Para sqlite3 com WAL é o comportamento correto. **Sugestão:** documentar o trade-off no docstring do lock.

### Evidências de Execução

```
$ python3 -m pytest tests/unit/test_batch_processor.py -v
10 passed in 0.73s

$ python3 -m pytest
283 passed, 2 skipped in 2.98s

$ ruff check src/batch/
All checks passed!
```

### Gate Status

Gate: PASS → docs/qa/gates/2.2-batch-processor-orquestrador.yml

